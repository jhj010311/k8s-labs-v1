apiVersion: apps/v1
kind: Deployment
metadata:
  name: frontend-deployment
spec:
  replicas: 2
  selector:
    matchLabels:
      app: frontend
  template:
    metadata:
      labels:
        app: frontend # Selector와 일치해야 함
    spec:
      containers:
      - name: frontend
        image: jhj010311/k8s-labs-todo-frontend:v1.0 # 본인의 이미지 주소로 변경
        ports:
        - containerPort: 3000